# Security Policy

## 沙箱安全

Kioxus 内置代码执行沙箱，安全是核心设计原则。

### 隔离机制

- **进程级隔离**：代码在独立子进程中执行，不影响主进程
- **静态代码分析**：执行前检查危险导入（os, subprocess, shutil）
- **4级安全策略**：STRICT / NORMAL / RELAXED / UNSAFE
- **资源限制**：超时、内存限制、输出大小限制

### 威胁模型

沙箱防护以下攻击：
- 恶意代码访问文件系统
- 恶意代码执行系统命令
- 恶意代码消耗过多资源（CPU/内存/时间）
- 恶意代码泄露敏感信息

### 已知限制

沙箱是进程级隔离，不是容器级隔离。对于高安全需求场景，建议在容器环境中运行。

## 报告安全漏洞

发现安全问题请通过 GitHub Issues 报告，标记为 security 标签。